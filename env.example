# ==============================================
# AudioGhost AI - Environment Configuration
# ==============================================
# Copy this file to .env and fill in your values
# 
# cp env.example .env
# ==============================================

# ===========================================
# HuggingFace Token (REQUIRED)
# ===========================================
# Get your token from: https://huggingface.co/settings/tokens
# You must first request access to: https://huggingface.co/facebook/sam-audio-large
HF_TOKEN=hf_your_token_here

# ===========================================
# Device Configuration (OPTIONAL)
# ===========================================
# Options: auto, cpu, cuda, mps
# - auto: Automatically detect best device (cuda > mps > cpu)
# - cpu: Force CPU mode (slower but works everywhere)
# - cuda: Force NVIDIA GPU (requires CUDA)
# - mps: Force Apple Metal (macOS only)
# DEVICE=auto

# ===========================================
# Model Configuration (OPTIONAL)
# ===========================================
# Default model size: small, base, large
# DEFAULT_MODEL_SIZE=base

# Model size to use when running on CPU (smaller = faster)
# CPU_DEFAULT_MODEL_SIZE=small

# ===========================================
# Processing Configuration (OPTIONAL)
# ===========================================
# Audio chunk duration in seconds (5-60)
# DEFAULT_CHUNK_DURATION=25.0

# Use float32 precision by default (better quality, more memory)
# DEFAULT_USE_FLOAT32=false

# ===========================================
# Task Configuration (OPTIONAL)
# ===========================================
# Maximum task duration in seconds (default: 1 hour)
# TASK_TIME_LIMIT=3600

# Result expiration time in seconds (default: 24 hours)
# RESULT_EXPIRES=86400

# Maximum upload file size in MB
# MAX_UPLOAD_SIZE_MB=500

# ===========================================
# Redis Configuration (OPTIONAL)
# ===========================================
# Only change these if using external Redis
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_DB=0

# ===========================================
# CORS Configuration (OPTIONAL)
# ===========================================
# Comma-separated list of allowed origins
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

